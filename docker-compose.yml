version: "3.8"

services:
  ecos:
    build: .
    image: ecos:latest
    container_name: ecos
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8000/ >/dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s

    # For local development with live code (optional), uncomment volume mount and reload flag
    # volumes:
    #   - ./:/app
    # command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
